const FilterButton = () => {
  return (
    <button className=" font-ibm cursor-pointer rounded-md shadow-md/20 w-full bg-primary py-2 text-white text-xs 2xl:text-sm font-semibold tracking-wider">
      Apply
    </button>
  );
};

export default FilterButton;
